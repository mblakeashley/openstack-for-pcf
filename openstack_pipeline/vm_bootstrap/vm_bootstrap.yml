---
platform: linux

image_resource:
  type: docker-image
  source: {repository: centos, tag: "centos6"}

inputs:
- name: openstack_vm_deploy

run:
  path: ./openstack_vm_deploy/openstack_pipeline/vm_bootstrap/vm_bootstrap.sh

params:
  USERNAME:
  PASSWORD:
